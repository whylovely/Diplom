# План доработки серверной части

## Текущая структура
- **Фреймворк**: ASP.NET Core 8
- **ORM**: Entity Framework Core
- **БД**: PostgreSQL
- **Контроллеры**: Auth, Accounts, Categories, Transactions
- **Entities**: User, Account, Category, Transaction, Entry

---

## Задачи

---

### 2. API обязательств
**Приоритет: Высокий** | **Время: 3-4 ч**

**Новые файлы:**
- `Entities/ObligationEntity.cs`
- `Controllers/ObligationsController.cs`
- `Shared/Obligations/ObligationDto.cs`

**Структура ObligationEntity:**
```csharp
public class ObligationEntity
{
    public Guid Id { get; set; }
    public Guid UserId { get; set; }
    public string Counterparty { get; set; }  // Контрагент
    public decimal Amount { get; set; }
    public string Currency { get; set; }
    public int Type { get; set; }  // 0 = Долг (я должен), 1 = Задолженность (мне должны)
    public DateTimeOffset CreatedAt { get; set; }
    public DateTimeOffset? DueDate { get; set; }  // Срок погашения
    public bool IsPaid { get; set; }
    public DateTimeOffset? PaidAt { get; set; }
    public string? Note { get; set; }
}
```

**Эндпоинты:**
- [x] `GET /api/obligations` — список обязательств
- [x] `POST /api/obligations` — создание
- [x] `PUT /api/obligations/{id}` — обновление
- [x] `DELETE /api/obligations/{id}` — удаление
- [x] `POST /api/obligations/{id}/pay` — отметить как оплаченное

> → Клиент: задача К3 «Окно обязательств» — ✅ выполнено

---

### 3. API курсов валют
**Приоритет: Высокий** | **Время: 2-3 ч**

**Новые файлы:**
- `Services/ExchangeRateService.cs`
- `Controllers/ExchangeController.cs`

**Что сделать:**
- [x] Интеграция с внешним API (ЦБ РФ или Open Exchange Rates)
- [x] Кэширование курсов (в памяти или Redis)
- [x] Эндпоинт `GET /api/exchange/rates?from=USD&to=RUB`
- [x] Поддержка криптовалют (BTC, ETH)

> → Клиент: К18 «Выбор базовой валюты» — ✅ выполнено, К20 «Автодополнение валют» — ⬜

---

### 4. Локальная БД PostgreSQL
**Приоритет: Средний** | **Время: 2-3 ч**

**Файлы:**
- `appsettings.json` — connection string
- `appsettings.Development.json` — локальные настройки
- `docker-compose.yml` — конфигурация PostgreSQL
- `db/init/` — скрипты инициализации

**Что сделать:**
- [ ] Настроить локальное подключение
- [ ] Документировать запуск через Docker
- [ ] Добавить seed-данные для тестирования
- [ ] Проверить все миграции

---

### 5. Улучшение API отчётов
**Приоритет: Низкий** | **Время: 2-3 ч**

**Что сделать:**
- [x] Эндпоинт для группированных данных по категориям
- [x] Агрегация на стороне сервера для ускорения

> → Клиент: К2 «Экспорт отчётов» — ✅ выполнено

---

### 6. Админ-панель
**Приоритет: Высокий** | **Время: 8-12 ч**

> [!IMPORTANT]
> Веб-интерфейс для администратора с управлением пользователями.

**Новые файлы:**
- `Controllers/AdminController.cs` — API для админа
- `Entities/AdminEntity.cs` — сущность администратора
- `wwwroot/` — статический веб-интерфейс (или отдельный проект)

**Функционал:**
- [ ] Роль администратора с отдельной авторизацией
- [ ] Просмотр списка пользователей
- [ ] Удаление/блокировка пользователей
- [ ] Редактирование данных пользователей
- [ ] Мониторинг изменений в БД (логирование)
- [ ] Шифрование данных пользователей (счета и транзакции)

**Что сделать:**
- [ ] Добавить middleware для проверки роли Admin
- [ ] Создать API эндпоинты `/api/admin/*`
- [ ] Реализовать AES-шифрование для чувствительных данных
- [ ] Создать простой веб-интерфейс (HTML/JS или Blazor)

---

### 7. Удалённый сервер и синхронизация
**Приоритет: Высокий** | **Время: 10-15 ч**

> [!WARNING]
> Комплексная задача: деплой + оффлайн-режим + синхронизация.

**Архитектура:**
```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   Клиент    │◄───────►│   Сервер    │◄───────►│  PostgreSQL │
│ (Avalonia)  │   API   │  (ASP.NET)  │         │   (Cloud)   │
│ + SQLite    │         │ render.com  │         │ Supabase/   │
│ (локально)  │         │ Railway и   │         │ ElephantSQL │
└─────────────┘         │ т.п.        │         └─────────────┘
                        └─────────────┘
```

**Серверная часть:**
- [ ] Настроить деплой на Render.com / Railway / Fly.io
- [ ] Настроить облачную PostgreSQL (Supabase / ElephantSQL / Neon)
- [ ] Добавить эндпоинты синхронизации:
  - `GET /api/sync/changes?since={timestamp}` — получить изменения
  - `POST /api/sync/push` — отправить локальные изменения
- [ ] Добавить `UpdatedAt`, `CreatedAt`, `IsDeleted` для всех сущностей
- [ ] Реализовать алгоритм разрешения конфликтов (Last Write Wins или ручной)

> → Клиент: К8 «Локальная БД», К9 «Синхронизация»

**Что настроить:**
- [ ] Environment variables для production
- [ ] HTTPS сертификат
- [ ] CORS для клиента
- [ ] Health check endpoint

---

## Миграции

После изменения Entities:
```bash
cd Server
dotnet ef migrations add <MigrationName>
dotnet ef database update
```

---

### 8. Проверка связей при soft delete (для админ-панели)
**Приоритет: Средний** | **Время: 2-3 ч**

> [!NOTE]
> Основная проверка перед удалением перенесена на клиента (задача К19) — ✅ выполнено.
> На сервере проверка остаётся для использования в админ-панели.

**Файлы:**
- `Controllers/AccountsController.cs` — проверка при soft delete
- `Controllers/CategoriesController.cs` — проверка при soft delete

**Что сделать:**
- [ ] В `AccountsController`: перед soft delete проверять наличие связанных Entries/Transactions
- [ ] В `CategoriesController`: перед soft delete проверять наличие связанных проводок
- [ ] Возвращать информативное сообщение с количеством связанных записей
- [ ] Для админа — разрешить принудительное удаление (параметр `?force=true`)

---

### 9. Rate-limiting запросов к API курсов валют
**Приоритет: Средний** | **Время: 1-2 ч**

**Файлы:**
- `Services/CbrExchangeRateService.cs` — кэширование и ограничение
- `Controllers/ExchangeController.cs` — логирование

**Что сделать:**
- [ ] Ограничить обращения к внешнему API (не чаще 1 раза в час)
- [ ] Кэшировать последние полученные курсы и отдавать из кэша
- [ ] Логировать обращения к API только в серверные логи (не показывать пользователю)
- [ ] При ошибке API — отдавать последние закэшированные данные

---

### 10. Тестирование и стабилизация
**Приоритет: Средний** | **Время: 4-6 ч**

**Что сделать:**
- [ ] Unit-тесты для основных сервисов (xUnit)
- [ ] Интеграционные тесты для контроллеров
- [ ] Проверка граничных случаев (Boundary testing)
- [ ] Исправление найденных багов

---

## Связь с клиентским планом

| Серверная задача | Зависимые клиентские задачи | Статус |
|---|---|---|
| С1 Многовалютные счета | Клиент: многовалютные счета | ✅ Оба выполнены |
| С2 API обязательств | К3 Окно обязательств | ✅ Оба выполнены |
| С3 API курсов валют | К18 Выбор валюты, К20 Автодополнение | Сервер ✅, Клиент (К18) ✅ |
| С5 API отчётов | К2 Экспорт отчётов | Сервер ✅, Клиент ✅ |
| С7 Удалённый сервер | К8 Локальная БД, К9 Синхронизация | Оба ⬜ |
| С8 Soft delete проверка | К19 Проверка перед удалением | Сервер ⬜, Клиент ✅ |

---

## Сводная таблица

| # |             Задача               | Приоритет |  Время  | Статус | Связь с клиентом |
|---|----------------------------------|-----------|---------|--------|------------------|
| 1 | Многовалютные счета              | Высокий   | 2-3 ч   | ✅ | Клиент ✅ |
| 2 | API обязательств                 | Высокий   | 3-4 ч   | ✅ | К3 ✅ |
| 3 | API курсов валют                 | Высокий   | 2-3 ч   | ✅ | К18 ✅, К20 ⬜ |
| 4 | Локальная БД                     | Средний   | 2-3 ч   | ⬜ | — |
| 5 | API отчётов                      | Низкий    | 2-3 ч   | ✅ | К2 ✅ |
| 6 | Админ-панель                     | Высокий   | 8-12 ч  | ⬜ | — |
| 7 | Удалённый сервер + синхронизация | Высокий   | 10-15 ч | ⬜ | К8, К9 |
| 8 | Проверка при soft delete (админ) | Средний   | 2-3 ч   | ⬜ | К19 ✅ |
| 9 | Rate-limiting API курсов | Средний | 1-2 ч | ⬜ | — |
| 10 | Тестирование и стабилизация | Средний | 4-6 ч | ⬜ | — |

---

## Общее время для сервера: ~37-56 ч (из них ~10 ч выполнено)
