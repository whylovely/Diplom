# План доработки серверной части

## Текущая структура
- **Фреймворк**: ASP.NET Core 8
- **ORM**: Entity Framework Core
- **БД**: PostgreSQL
- **Контроллеры**: Auth, Accounts, Categories, Transactions, Obligations, Exchange, Report, Admin
- **Entities**: User, Account, Category, Transaction, Entry, Obligation

---

## ✅ Выполненные задачи

---

### С1. Многовалютные счета ✅
**Время: 2-3 ч**

Поддержка нескольких валют в счетах (`Currency`, `SecondaryCurrency`, `ExchangeRate`).

> → Клиент: многовалютные счета — ✅ выполнено

---

### С2. API обязательств ✅
**Время: 3-4 ч**

**Файлы:** `Entities/ObligationEntity.cs`, `Controllers/ObligationsController.cs`, `Shared/Obligations/ObligationDto.cs`

**Эндпоинты:**
- [x] `GET /api/obligations` — список обязательств
- [x] `POST /api/obligations` — создание
- [x] `PUT /api/obligations/{id}` — обновление
- [x] `DELETE /api/obligations/{id}` — удаление
- [x] `POST /api/obligations/{id}/pay` — отметить как оплаченное

> → Клиент: К3 «Окно обязательств» — ✅ выполнено

---

### С3. API курсов валют ✅
**Время: 2-3 ч**

**Файлы:** `Services/CbrExchangeRateService.cs`, `Controllers/ExchangeController.cs`

- [x] Интеграция с ЦБ РФ + CoinGecko (крипто)
- [x] Кэширование курсов (IMemoryCache, 1 ч TTL)
- [x] Эндпоинт `GET /api/exchange/rates`
- [x] Поддержка криптовалют (BTC, ETH, USDT)

> → Клиент: К18 «Выбор базовой валюты» — ✅, К20 «Автодополнение валют» — ⬜

---

### С5. API отчётов ✅
**Время: 2-3 ч**

**Файлы:** `Shared/Reports/ReportDtos.cs`, `Controllers/ReportController.cs`

- [x] `GET /api/reports/summary` — сводка + группировка по категориям
- [x] `GET /api/reports/by-category` — расходы/доходы по категориям
- [x] `GET /api/reports/monthly` — помесячные итоги
- [x] `GET /api/reports/turnover` — обороты по счетам

> Клиентская агрегация сохранена для оффлайн-режима.
> → Клиент: К2 «Экспорт отчётов» — ✅ выполнено

---

### С6. Админ-панель ✅
**Время: 8-12 ч**

**Файлы:** `Controllers/AdminController.cs`, `Entities/UserEntity.cs` (Role, IsBlocked), `wwwroot/admin/index.html`

**Функционал (сделано):**
- [x] Роль администратора с отдельной авторизацией (только веб)
- [x] `POST /api/auth/admin/login` — вход для админа
- [x] Просмотр списка пользователей
- [x] Удаление/блокировка пользователей
- [x] Веб-интерфейс (HTML/JS, dark theme)
- [x] Seed-админ из `appsettings.json`

**Функционал (отложено):**
- [ ] Редактирование данных пользователей
- [ ] Мониторинг изменений в БД (логирование)
- [ ] AES-шифрование данных пользователей

---

### С8. Проверка связей при soft delete (для админ-панели)
**Приоритет: Средний** | **Время: 2-3 ч**

> [!NOTE]
> Основная проверка перед удалением перенесена на клиента (задача К19) — ✅ выполнено.
> На сервере проверка остаётся для использования в админ-панели.

**Файлы:**
- `Controllers/AccountsController.cs` — проверка при soft delete
- `Controllers/CategoriesController.cs` — проверка при soft delete

**Что сделать:**
- [x] В `AccountsController`: перед soft delete проверять наличие связанных Entries
- [x] В `CategoriesController`: перед soft delete проверять наличие связанных проводок
- [x] Возвращать информативное сообщение с количеством связанных записей
- [x] Для админа — разрешить принудительное удаление (параметр `?force=true`)

---

### С9. Rate-limiting API курсов ✅
**Время: 1-2 ч**

**Файлы:** `Services/CbrExchangeRateService.cs`

- [x] Кэш 1 час (IMemoryCache) — не чаще 1 запроса в час к внешним API
- [x] Fallback-кэш 7 дней (LastKnown) — при ошибке API отдаёт устаревшие данные
- [x] `ILogger` вместо Console.WriteLine — логи только серверные

---

## 🔴 Оставшиеся задачи

---

### С4. Локальная БД PostgreSQL
**Приоритет: Средний** | **Время: 2-3 ч**

**Файлы:**
- `appsettings.json` — connection string
- `appsettings.Development.json` — локальные настройки
- `docker-compose.yml` — конфигурация PostgreSQL
- `db/init/` — скрипты инициализации

**Что сделать:**
- [ ] Настроить локальное подключение
- [ ] Документировать запуск через Docker
- [ ] Добавить seed-данные для тестирования
- [ ] Проверить все миграции

---

### С7. Удалённый сервер и синхронизация
**Приоритет: Высокий** | **Время: 10-15 ч**

> [!WARNING]
> Комплексная задача: деплой + оффлайн-режим + синхронизация.

**Архитектура:**
```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   Клиент    │◄───────►│   Сервер    │◄───────►│  PostgreSQL │
│ (Avalonia)  │   API   │  (ASP.NET)  │         │   (Cloud)   │
│ + SQLite    │         │ render.com  │         │ Supabase/   │
│ (локально)  │         │ Railway и   │         │ ElephantSQL │
└─────────────┘         │ т.п.        │         └─────────────┘
                        └─────────────┘
```

**Серверная часть:**
- [ ] Настроить деплой на Render.com / Railway / Fly.io
- [ ] Настроить облачную PostgreSQL (Supabase / ElephantSQL / Neon)
- [ ] Добавить эндпоинты синхронизации:
  - `GET /api/sync/changes?since={timestamp}` — получить изменения
  - `POST /api/sync/push` — отправить локальные изменения
- [ ] Добавить `UpdatedAt`, `CreatedAt`, `IsDeleted` для всех сущностей
- [ ] Реализовать алгоритм разрешения конфликтов (Last Write Wins или ручной)

> → Клиент: К8 «Локальная БД», К9 «Синхронизация»

**Что настроить:**
- [ ] Environment variables для production
- [ ] HTTPS сертификат
- [ ] CORS для клиента
- [ ] Health check endpoint

---

### С10. Тестирование и стабилизация
**Приоритет: Средний** | **Время: 4-6 ч**

**Что сделать:**
- [ ] Unit-тесты для основных сервисов (xUnit)
- [ ] Интеграционные тесты для контроллеров
- [ ] Проверка граничных случаев (Boundary testing)
- [ ] Исправление найденных багов

---

## Сводная таблица

| # |             Задача               | Приоритет |  Время  | Статус | Связь с клиентом |
|---|----------------------------------|-----------|---------|--------|------------------|
| 4 | Локальная БД                     | Средний   | 2-3 ч   | ⬜ | — |
| 7 | Удалённый сервер + синхронизация | Высокий   | 10-15 ч | ⬜ | К8, К9 |
| 10 | Тестирование и стабилизация     | Средний   | 4-6 ч   | ⬜ | — |

**Осталось: 3 задачи (~16-24 ч)**