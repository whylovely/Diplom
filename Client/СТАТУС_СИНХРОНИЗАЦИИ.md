# К9 — Синхронизация: текущий статус

## ✅ Что сделано

1. ✅ `ApiService.cs` — HTTP-клиент с JWT для всех REST API
2. ✅ `SyncService.cs` — pull с сервера + маппинг DTO → Models
3. ✅ `LocalDbService.ReplaceAllData()` — запись в SQLite
4. ✅ Кнопка синхронизации в окне «Счета»
5. ✅ `GET /api/transactions` на сервере (добавлен)
6. ✅ Технические счета для категорий создаются при синхронизации
7. ✅ Пересчёт балансов из транзакций
8. ✅ Отображение реального сообщения об ошибке в UI
9. ✅ Демо-данные пересозданы с правильными Direction
10. ✅ Сервер перезапущен с новым endpoint

## 🔴 Нерешённые баги после синхронизации

### К29. Окно отчётов не работает (все вкладки)
- Все вкладки отчётов пустые после синхронизации
- Нужно проверить `ReportViewModel` — загрузка данных и подписка на `DataChanged`

### К30. Окно категорий пустое
- Категории не отображаются в `CategoriesView` после синхронизации
- Нужно проверить привязку технических счетов к категориям

## Следующие шаги

1. Исправить баг К30 (категории пустые)
2. Исправить баг К29 (отчёты не работают)
3. Финальная проверка синхронизации



Сделать чтобы было окно при синхронизации, показывало крайнюю дату изменения данных. и был выбор взять данные с сервера или отправить данные с клиента на сервер. Такое окно должно появляться если произошел конфликт данных, теперь клиент-first бд.